export enum IpcMessages {
    WindowAction = "window-action-message",
    WindowTitle = "window-title",
}

export enum WindowActions {
    Minimaze = "minimaze",
    Maximaze = "toggle-maximaze",
    Close = "close",
}